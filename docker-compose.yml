---
version: '2'

services:
  node:
    build:
      context: .
      dockerfile: Dockerfile-node
    ports:
      - "8080"
    volumes:
      - "./node:/usr/src/app/my"
    working_dir: /usr/src/app
    command: "node my/myfirst.js"
